---
id: 05-02
phase: "05"
title: "Testimonial card — next/image headshot + LinkedIn verification badge"
wave: 2
depends_on:
  - 05-01
files_modified:
  - components/sections/Testimonials.tsx
autonomous: true
requirements:
  - TEST-05
  - TEST-06
---

# Plan 05-02: Testimonial card — next/image headshot + LinkedIn verification badge

## Objective
Deliver the presentation slice: update `components/sections/Testimonials.tsx` so each card renders a circular headshot via `next/image` (replacing the plain `<img>`) and shows a muted `FaLinkedin` badge link in the attribution row for entries where `source === 'linkedin' && linkedinUrl`. The card stays a Server Component, the 3-column grid is frozen (carousel is Phase 6), and the `getInitials` fallback is preserved for any entry missing a photo. Depends on Plan 05-01 because the component reads the new `photo`/`source`/`linkedinUrl` fields.

## Wave
Wave 2

## Tasks

<task id="05-02-T1">
<title>Swap avatar to next/image circular headshot, keep initials fallback</title>
<read_first>
- components/sections/Testimonials.tsx (current: plain `<img>` on `t.avatarUrl`, initials fallback, `min-w-0` name wrapper)
- lib/data/testimonials.ts (post-05-01 interface — confirm `photo` and `source` field names)
- .planning/phases/05-testimonials-data-cards/05-UI-SPEC.md (Component Contract 1 lines 97–114; Attribution Row Structure lines 136–150)
- .planning/phases/05-testimonials-data-cards/05-RESEARCH.md (component changes lines 104–153)
</read_first>
<action>
Add `import Image from 'next/image'` at the top of `components/sections/Testimonials.tsx`. Keep the file a Server Component (do NOT add `'use client'`). In the attribution row, replace the `t.avatarUrl ?` conditional with `t.photo ?`. In the truthy branch, replace the `<img>` element with a `<Image>` element using `src={t.photo}`, `alt={t.name}`, `width={40}`, `height={40}`, and `className="w-10 h-10 rounded-full object-cover shrink-0"`. Leave the falsy branch (the `getInitials` initials `<div>`) exactly as-is so it still renders for any entry without a photo. Change the name/title wrapper `div` className from `min-w-0` to `min-w-0 flex-1` so its `truncate` text shrinks correctly when the badge sits to its right. Do not change the section wrapper, the `// testimonials` label, the grid (`grid-cols-1 md:grid-cols-3 gap-6`), the card shell, the quote `<p>`, or the `FadeIn` stagger (`delay={index * 0.1}`).
</action>
<acceptance_criteria>
- [ ] `components/sections/Testimonials.tsx` contains `import Image from 'next/image'`
- [ ] The file does NOT contain `'use client'`
- [ ] The avatar conditional keys on `t.photo` (not `t.avatarUrl`); `grep -c "avatarUrl" components/sections/Testimonials.tsx` returns 0
- [ ] An `<Image` element exists with `width={40}`, `height={40}`, `alt={t.name}`, and `className="w-10 h-10 rounded-full object-cover shrink-0"`
- [ ] The initials fallback `<div>` using `getInitials(t.name)` is still present
- [ ] The name/title wrapper div className is `min-w-0 flex-1`
- [ ] The grid wrapper still reads `grid grid-cols-1 md:grid-cols-3 gap-6`
- [ ] `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="05-02-T2">
<title>Add conditional LinkedIn badge link to the attribution row</title>
<read_first>
- components/sections/Testimonials.tsx (attribution row after T1's edits)
- components/sections/Contact.tsx (reference: `FaLinkedin` import from `react-icons/fa`, `target="_blank" rel="noopener noreferrer"` pattern)
- .planning/phases/05-testimonials-data-cards/05-UI-SPEC.md (Component Contract 2 lines 116–134; Accessibility Contract lines 200–206)
- .planning/phases/05-testimonials-data-cards/05-CONTEXT.md (D-03/D-04/D-05 badge placement, icon-only, conditional)
</read_first>
<action>
Add `import { FaLinkedin } from 'react-icons/fa'` to `components/sections/Testimonials.tsx` (same import path used in `Contact.tsx` — note `SiLinkedin` does not exist in react-icons v5). Inside the attribution row `<div className="flex items-center gap-3 ...">`, immediately after the name/title wrapper div, add a conditional that renders only when `t.source === 'linkedin' && t.linkedinUrl`. The rendered element is an anchor: `href={t.linkedinUrl}`, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label="View on LinkedIn"`, `className="ml-auto text-[#a3a3a3] hover:text-[#f5f5f5] transition-colors duration-150 shrink-0"`, containing `<FaLinkedin size={16} />`. The `ml-auto` pushes the icon to the right edge of the flex row; the badge is icon-only with no visible text label. Entries with `source: 'direct'` or no `linkedinUrl` render no badge.
</action>
<acceptance_criteria>
- [ ] `components/sections/Testimonials.tsx` contains `import { FaLinkedin } from 'react-icons/fa'`
- [ ] A conditional gated on `t.source === 'linkedin' && t.linkedinUrl` wraps the badge anchor
- [ ] The anchor has `target="_blank"` and `rel="noopener noreferrer"`
- [ ] The anchor has `aria-label="View on LinkedIn"`
- [ ] The anchor className includes `ml-auto`, `text-[#a3a3a3]`, `hover:text-[#f5f5f5]`, and `transition-colors duration-150`
- [ ] The anchor renders `<FaLinkedin size={16} />`
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npx next build` completes without errors
</acceptance_criteria>
</task>

## Verification
1. `npx tsc --noEmit` exits 0 — type-correct against the post-05-01 `TestimonialItem`.
2. `npx next build` completes without errors (confirms `next/image` usage and the Server Component compile cleanly).
3. `grep -c "avatarUrl" components/sections/Testimonials.tsx` returns 0; `grep -c "'use client'" components/sections/Testimonials.tsx` returns 0.
4. Running `next dev`: each card shows a circular headshot (or initials fallback for any photo-less entry); cards with `source: 'linkedin'` show a muted LinkedIn icon at the right edge of the attribution row that opens the recommendation in a new tab; `source: 'direct'` cards show no badge.
5. The 3-column grid layout is unchanged from before the phase.

## must_haves
truths:
  - Testimonials.tsx remains a Server Component (no 'use client')
  - Each card renders a circular next/image headshot (width/height 40, rounded-full, object-cover) instead of the plain img tag
  - The getInitials fallback still renders for any entry missing a photo
  - LinkedIn-sourced cards (source 'linkedin' and linkedinUrl present) show a FaLinkedin icon-only link with aria-label "View on LinkedIn", target _blank, and rel noopener noreferrer
  - direct-source cards render no LinkedIn badge
  - The 3-column grid and FadeIn stagger are unchanged
  - tsc --noEmit and next build both pass

## Artifacts this phase produces
- modified: `components/sections/Testimonials.tsx` — adds `import Image from 'next/image'` and `import { FaLinkedin } from 'react-icons/fa'`; swaps `<img>`/`t.avatarUrl` for `<Image>`/`t.photo`; changes name wrapper to `min-w-0 flex-1`; adds conditional LinkedIn badge anchor

<threat_model>
**Phase 05 Threat Model (ASVS L1) — Plan 05-02**
- Renders only public, static data from `lib/data/testimonials.ts`. No user input, no auth, no server-side logic.
- External-link safety: every LinkedIn badge anchor uses `target="_blank"` with `rel="noopener noreferrer"` to prevent reverse-tabnabbing and referrer leakage (matches the established `Contact.tsx` pattern).
- `href={t.linkedinUrl}` is a developer-authored static string — no user-controlled URL injection; values should be legitimate `https://` LinkedIn URLs (no `javascript:` schemes), which is enforced at data-authoring time in Plan 05-01.
- `next/image` serves local `/public/` assets only — no remote image fetch surface, no SSRF.
- No XSS: all rendered values are static strings; no `dangerouslySetInnerHTML`.
- Risk level: LOW.
</threat_model>
