---
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components/ui/Button.tsx
  - components/ui/NavBar.tsx
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
---

# Wave 1 — Scaffold + Walking Skeleton

**Goal:** Next.js 15 project scaffolded, dark theme configured via CSS custom properties, Geist fonts loaded and self-hosted, minimal page rendering the hero shell (content only, no Framer Motion animation yet). Proves the full local dev loop end-to-end.

**Requirements covered:** FOUND-01, FOUND-02, FOUND-03

---

## must_haves

```yaml
must_haves:
  truths:
    - "next dev runs without TypeScript errors"
    - "Dark background (#1a1a1a) renders on page load with no flash of white/light theme"
    - "Hero shell displays name, role descriptor, tagline placeholder, and CTA with correct typography"
    - "next build exits with code 0"
    - "tsc --noEmit exits with code 0"
  prohibitions:
    - statement: "No JS class toggle for dark mode"
      status: resolved
      verification: "globals.css sets background-color on html element as plain CSS, not inside @layer"
    - statement: "No component libraries (shadcn, radix, etc.)"
      status: resolved
      verification: "package.json contains no shadcn, @radix-ui, or @headlessui dependencies"
    - statement: "No Tailwind v3 config pattern (tailwind.config.ts with theme.extend)"
      status: resolved
      verification: "All custom tokens live in @theme block in globals.css; no tailwind.config.ts with extend"
```

---

## Artifacts this phase produces

- `package.json` — dependencies: next@15, react@19, framer-motion, react-icons; devDeps: typescript, tailwindcss@4
- `next.config.ts` — minimal Next.js 15 config
- `tsconfig.json` — strict TypeScript, `@/*` path alias
- `app/layout.tsx` — root layout: Geist + Geist Mono fonts, metadata, CSS variable injection
- `app/page.tsx` — page shell: NavBar + hero section shell + section anchors
- `app/globals.css` — Tailwind v4 @import, @theme tokens, html background flash fix
- `components/ui/Button.tsx` — named export `Button`, ghost/outline anchor CTA
- `components/ui/NavBar.tsx` — named export `NavBar`, fixed glassmorphism nav

---

<threat_model>
Phase 1 scope: static site scaffold + hero section shell. No user input, no auth, no DB, no API routes.
Attack surface: none in this phase. The contact form (Phase 3) is the only future attack surface.
ASVS Level 1 checks applicable to this phase:
- Dependency audit: run `npm audit` after install — zero high or critical vulnerabilities required
- No secrets in source code or committed .env files — verify .gitignore before first push
High-severity threats: none applicable to a static scaffold phase.
</threat_model>

---

## Tasks

<task id="T1" title="Scaffold Next.js 15 project with TypeScript and Tailwind CSS v4">
  <read_first>
    - .planning/research/PITFALLS.md — Tailwind v4 CSS-first config pitfall (do not put tokens in tailwind.config.ts); dark mode flash prevention
    - .planning/research/ARCHITECTURE.md — folder structure reference; match exactly
  </read_first>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"` in the project root. Accept all defaults.
    After scaffolding, install additional dependencies: `npm install framer-motion react-icons`.
    Run `npm audit` — if any high or critical vulnerabilities are found, run `npm audit fix` before proceeding.
    Delete `tailwind.config.ts` if create-next-app generated one — Tailwind v4 uses CSS-first config only; the file will be recreated empty or not needed.
    Delete the contents of `app/globals.css` — it will be fully replaced in T2.
    Delete the default content of `app/page.tsx` — it will be replaced in T4.
    Verify `tsconfig.json` contains `"paths": { "@/*": ["./*"] }` under `compilerOptions`.
    Verify `package.json` dependencies include: `next` (^15.x), `react` (^19.x), `react-dom` (^19.x), `framer-motion`, `react-icons`.
    Verify `package.json` devDependencies include: `typescript`, `@types/node`, `@types/react`, `@types/react-dom`, `tailwindcss` (v4.x).
  </action>
  <acceptance_criteria>
    - `package.json` lists framer-motion and react-icons in dependencies
    - `npm audit` shows zero high or critical severity vulnerabilities
    - `tailwind.config.ts` does not exist OR is empty with no extend/theme keys
    - `tsconfig.json` contains `"@/*": ["./*"]` path alias
    - `next.config.ts` exists at project root
  </acceptance_criteria>
</task>

<task id="T2" title="Configure globals.css with Tailwind v4 tokens and dark background flash prevention">
  <read_first>
    - .planning/phases/01-foundation-hero/01-UI-SPEC.md — Tailwind v4 Token Configuration section (exact @theme block with all tokens)
    - .planning/research/PITFALLS.md — Dark mode flash (FOUC) prevention; plain CSS on html element, NOT inside @layer or @theme
  </read_first>
  <action>
    Replace the full contents of `app/globals.css` with these sections in this exact order:

    Section 1 — Tailwind v4 import directive:
    `@import "tailwindcss";`

    Section 2 — Design token declarations inside @theme block:
    `@theme { --color-bg: #1a1a1a; --color-surface: #242424; --color-text: #f5f5f5; --color-text-muted: #a3a3a3; --color-accent: #c8964a; --font-sans: var(--font-geist); --font-mono: var(--font-geist-mono); }`

    Section 3 — Flash prevention (plain CSS, NOT inside @layer or @theme):
    `html { background-color: #1a1a1a; color: #f5f5f5; }`

    Section 4 — Box model reset:
    `*, *::before, *::after { box-sizing: border-box; }`

    Section 5 — Body base styles:
    `body { font-family: var(--font-geist), system-ui, sans-serif; font-weight: 300; line-height: 1.7; -webkit-font-smoothing: antialiased; }`

    The html background-color rule in Section 3 must be a standalone CSS rule — not nested inside @layer base, @theme, or any other at-rule. This is critical: it must apply before Tailwind's layer cascade and before JavaScript loads.
  </action>
  <acceptance_criteria>
    - `app/globals.css` first line is `@import "tailwindcss";`
    - `app/globals.css` contains an `@theme {}` block with all five --color-* tokens and two --font-* tokens
    - `app/globals.css` contains a standalone `html { background-color: #1a1a1a; }` rule outside any @layer or @theme block
    - Loading the page in a browser with JavaScript disabled (DevTools → Settings → Disable JavaScript) still shows a dark #1a1a1a background
    - No `@layer base` block contains the html background-color override
  </acceptance_criteria>
</task>

<task id="T3" title="Load Geist and Geist Mono fonts via next/font/google in app/layout.tsx">
  <read_first>
    - .planning/research/ARCHITECTURE.md — Font loading pattern; CSS variable approach for next/font
    - .planning/research/PITFALLS.md — Font loading pitfall; load only weights actually used (300, 400, 500, 700 for Geist; 400 for Geist Mono)
    - .planning/phases/01-foundation-hero/01-UI-SPEC.md — Typography table; weights used per role
  </read_first>
  <action>
    Replace `app/layout.tsx` with a Server Component containing:

    Imports: `Geist` and `Geist_Mono` from `next/font/google`; `type Metadata` from `next`; `./globals.css`.

    Font configuration:
    - `const geist = Geist({ subsets: ['latin'], variable: '--font-geist', weight: ['300', '400', '500', '700'] })`
    - `const geistMono = Geist_Mono({ subsets: ['latin'], variable: '--font-geist-mono', weight: ['400'] })`

    Metadata export:
    - `title: 'Nicholas William — Freelance Software Engineer'`
    - `description: 'Freelance software engineer building fast, scalable web products. Available for projects.'`

    RootLayout component:
    - `<html lang="en" className={`${geist.variable} ${geistMono.variable}`}>`
    - `<body>{children}</body>`
    - No className on body — font-family is set via CSS in globals.css
    - No extra wrapper divs
  </action>
  <acceptance_criteria>
    - DevTools Network tab (filter: Font) after a hard refresh shows all Geist files served from `/_next/static/media/` — zero requests to `fonts.googleapis.com`
    - The `<html>` element in browser DOM has a className containing both `__variable_` font identifiers (Geist and Geist Mono)
    - Page `<title>` in browser tab reads "Nicholas William — Freelance Software Engineer"
    - `tsc --noEmit` passes with zero errors
  </acceptance_criteria>
</task>

<task id="T4" title="Create components/ui/Button.tsx — ghost CTA anchor button">
  <read_first>
    - .planning/phases/01-foundation-hero/01-UI-SPEC.md — CTA Button Contract section (exact Tailwind classes, hover state, border radius, focus ring)
  </read_first>
  <action>
    Create `components/ui/` directory if it does not exist.
    Create `components/ui/Button.tsx` as a Server Component (no 'use client' — it renders a static anchor).
    Export a named function `Button` accepting props: `href: string`, `children: React.ReactNode`, `className?: string`.
    Render an `<a>` element with:
    - `href` passed through
    - Fixed className: `inline-block border border-[#c8964a] text-[#c8964a] px-6 py-3 rounded-sm tracking-wide text-[15px] font-medium transition-colors duration-200 hover:bg-[#c8964a]/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#c8964a] focus-visible:ring-offset-2 focus-visible:ring-offset-[#1a1a1a]`
    - Any `className` prop appended after the fixed classes
    - `children` rendered inside
    Use a `<button>` element only if the action is non-navigational. This CTA is always a link (smooth-scroll), so use `<a>`.
  </action>
  <acceptance_criteria>
    - `components/ui/Button.tsx` exists and exports a named `Button` component
    - The component renders as an `<a>` tag in the DOM (not `<button>`)
    - Button shows amber (#c8964a) border and amber text against the dark background
    - Hovering shows a very subtle amber fill (bg-[#c8964a]/10) — no background flash or jump
    - Tab-focusing the button renders a visible amber ring (focus-visible:ring-2)
    - `tsc --noEmit` passes
  </acceptance_criteria>
</task>

<task id="T5" title="Create components/ui/NavBar.tsx — fixed glassmorphism navigation bar">
  <read_first>
    - .planning/phases/01-foundation-hero/01-UI-SPEC.md — Navigation Bar layout contract (exact classes: fixed top-0, bg-[#1a1a1a]/80, backdrop-blur-md, border-b border-white/5, h-16)
    - .planning/phases/01-foundation-hero/01-CONTEXT.md — D-17 nav links (About, Skills, Work, Contact); D-19 no mono in nav; D-18 backdrop blur
  </read_first>
  <action>
    Create `components/ui/NavBar.tsx`.
    Mark with `'use client'` — keeps the door open for active-link scroll detection in a future phase without refactoring.
    Render a `<header>` with className `fixed top-0 left-0 right-0 z-50 h-16 bg-[#1a1a1a]/80 backdrop-blur-md border-b border-white/5`.
    Inside, a `<nav>` with className `max-w-5xl mx-auto px-6 h-full flex items-center justify-between`.
    Left side: `<a href="#top" className="text-sm font-medium text-[#f5f5f5] hover:text-[#c8964a] transition-colors duration-200">NW</a>`
    Right side: `<ul className="flex items-center gap-8 list-none m-0 p-0">` with four `<li>` elements, each containing:
    - `href="#about"` text "About"
    - `href="#skills"` text "Skills"
    - `href="#work"` text "Work"
    - `href="#contact"` text "Contact"
    Each nav `<a>`: className `text-sm font-medium text-[#a3a3a3] hover:text-[#c8964a] transition-colors duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#c8964a] rounded-sm`
    Font is Geist (inherited from body) — not Geist Mono (mono is section labels only per D-03/D-19).
  </action>
  <acceptance_criteria>
    - NavBar is fixed at top of viewport — does not scroll with page content
    - Backdrop blur is visible when content scrolls beneath the nav
    - Four nav links (About, Skills, Work, Contact) visible on the right; "NW" on the left
    - Nav link font is Geist (not Geist Mono) — verifiable in DevTools Computed styles font-family
    - Hovering a nav link changes color to amber (#c8964a)
    - Tab-focusing a nav link shows a visible focus ring
    - `tsc --noEmit` passes
  </acceptance_criteria>
</task>

<task id="T6" title="Create app/page.tsx — page shell with hero content and section anchors">
  <read_first>
    - .planning/research/ARCHITECTURE.md — page.tsx is a Server Component composing sections
    - .planning/phases/01-foundation-hero/01-UI-SPEC.md — Hero Section layout contract (h-[75vh], flex flex-col justify-center, max-w-5xl mx-auto px-6, max-w-2xl content)
    - .planning/phases/01-foundation-hero/01-CONTEXT.md — D-11 left-aligned; D-14 content stack order
  </read_first>
  <action>
    Replace `app/page.tsx` with a Server Component (no 'use client').
    Import `NavBar` from `@/components/ui/NavBar` and `Button` from `@/components/ui/Button`.
    Render:
    1. `<NavBar />`
    2. `<main id="top">` containing:
       a. `<section id="hero" className="h-[75vh] flex flex-col justify-center">` containing a `<div className="max-w-5xl mx-auto px-6">` containing a `<div className="max-w-2xl">` with these five static elements (no animation in Wave 1):
          - `<p className="font-mono text-xs text-[#a3a3a3] tracking-widest uppercase mb-6">// freelance</p>`
          - `<h1 className="font-bold text-[#f5f5f5] mb-2" style={{ fontSize: 'clamp(48px, 6vw + 8px, 72px)', lineHeight: 1.1, letterSpacing: '-0.02em' }}>Nicholas William</h1>`
          - `<p className="text-lg font-light text-[#a3a3a3] tracking-wide mb-4">Freelance Software Engineer</p>`
          - `<p className="text-base font-light text-[#f5f5f5] mb-8">Building fast, scalable web products for ambitious teams.</p>`
          - `<Button href="#contact">Get in touch</Button>`
       b. Empty section anchors for future phases (after the hero section, inside `<main>`):
          `<div id="about" />`, `<div id="skills" />`, `<div id="work" />`, `<div id="contact" />`
    The `font-mono` class in the mono label relies on the `--font-mono` token set in globals.css via the Geist Mono CSS variable — verify this renders in Geist Mono.
    No Framer Motion imports in this file — animation is added in Wave 2.
  </action>
  <acceptance_criteria>
    - `app/page.tsx` has no 'use client' directive
    - `next dev` serves the page without TypeScript errors or React console warnings
    - Hero section visible: `// freelance` label in muted gray, name in large bold off-white, role in lighter gray, tagline in off-white, "Get in touch" button with amber border
    - Page source contains all five section anchor ids: hero, about, skills, work, contact
    - No Framer Motion imports exist in app/page.tsx
    - `tsc --noEmit` passes
  </acceptance_criteria>
</task>

<task id="T7" title="Verify Wave 1 walking skeleton end-to-end">
  <read_first>
    - .planning/phases/01-foundation-hero/01-SKELETON.md — Definition of Done checklist (first 5 items apply to Wave 1)
    - .planning/research/PITFALLS.md — dark mode flash; font requests; TypeScript strictness
  </read_first>
  <action>
    Run `tsc --noEmit` — must exit with code 0.
    Run `next build` — must exit with code 0 with zero TypeScript and zero ESLint errors.
    Run `next dev` and open http://localhost:3000.
    Hard refresh three times (Ctrl+Shift+R): background must be dark (#1a1a1a) from first paint — no white flash.
    Open DevTools Network tab → filter Font → reload: all Geist font files must come from `/_next/static/media/`, zero requests to `fonts.googleapis.com`.
    Inspect `<html>` in DevTools Elements: className must include two `__variable_` tokens (Geist and Geist Mono CSS variables).
    Inspect the `// freelance` label: font-family must show Geist Mono (monospace) in Computed styles.
    Tab through the page: NavBar links and "Get in touch" button must all show visible focus rings.
    Click "Get in touch": page must scroll toward `#contact` anchor.
    If any check fails, fix before Wave 2.
  </action>
  <acceptance_criteria>
    - `tsc --noEmit` exits with code 0
    - `next build` exits with code 0, zero errors
    - No white flash on hard reload (verified across 3 refreshes)
    - All font requests from `/_next/static/media/` — zero from `fonts.googleapis.com`
    - `// freelance` label renders in Geist Mono (monospace) font
    - All interactive elements show focus rings on Tab navigation
    - "Get in touch" button click triggers scroll behavior
  </acceptance_criteria>
</task>
