---
wave: 1
depends_on: []
files_modified:
  - lib/data/about.ts
  - components/sections/About.tsx
  - app/page.tsx
autonomous: true
requirements:
  - ABOUT-01
---

# Wave 1 — About Section

**Goal:** Implement the About section with a short prose bio, a `// about` mono label, FadeIn scroll animation, and integrate it into `app/page.tsx`. The section replaces the existing empty `#about` anchor.

**Requirements covered:** ABOUT-01

---

## must_haves

```yaml
must_haves:
  truths:
    - "About section renders at id='about' with 2–4 sentences of prose bio"
    - "Section label '// about' uses Geist Mono, #a3a3a3, 12px, tracking-widest"
    - "Bio text uses Geist, font-light (300), #f5f5f5, 16px, line-height 1.7"
    - "FadeIn wrapper triggers on scroll entry (whileInView, once: true)"
    - "tsc --noEmit exits 0; next build exits 0"
  prohibitions:
    - statement: "No 'use client' on About.tsx itself — use FadeIn wrapper pattern"
      status: resolved
      verification: "About.tsx has no 'use client' directive; FadeIn.tsx wraps the animated content"
    - statement: "No hardcoded colors — use CSS vars or Tailwind tokens from globals.css"
      status: resolved
      verification: "All color values reference --color-text, --color-text-muted, etc."
```

---

## Artifacts this phase produces

- `lib/data/about.ts` — `AboutData` interface + `aboutData` const with bio string
- `components/sections/About.tsx` — Server Component; `// about` mono label, prose bio wrapped in `<FadeIn>`
- `app/page.tsx` — updated to import and render `<About />` after `<Hero />`; remove empty `#about` anchor

---

## Tasks

### T1 — Create `lib/data/about.ts`

Create typed data file:

```ts
export interface AboutData {
  bio: string;
}

export const aboutData: AboutData = {
  bio: "Nicholas Willian is a freelance software engineer specialising in full-stack web applications. He helps ambitious teams design and ship reliable, scalable products — from architecture through deployment. Available for project-based and retainer engagements.",
};
```

Commit: `feat(02-01): create lib/data/about.ts with typed AboutData`

---

### T2 — Create `components/sections/About.tsx`

Server Component. Layout:

```
[section id="about", py-24]
  [max-w-5xl mx-auto px-6]
    [max-w-2xl]
      <p class="mono label">// about</p>
      <FadeIn>
        <p class="prose bio">{aboutData.bio}</p>
      </FadeIn>
```

- Section label: `font-mono text-xs text-[#a3a3a3] tracking-widest uppercase mb-6`
- Bio: `text-base font-light text-[#f5f5f5] leading-relaxed`
- `py-24` vertical padding for section breathing room

Commit: `feat(02-01): create components/sections/About.tsx`

---

### T3 — Update `app/page.tsx`

Import `About` and render it after `<Hero />`. Remove the empty `<section id="about" />` stub if present.

Commit: `feat(02-01): add About section to app/page.tsx`

---

### T4 — Verify

Run `tsc --noEmit` and `next build`. Both must exit 0.

Commit: (no commit needed — verification only)

---

## Success criteria

- [ ] `lib/data/about.ts` exports `AboutData` interface and `aboutData` const
- [ ] `components/sections/About.tsx` is a Server Component using `<FadeIn>`
- [ ] `// about` mono label renders with correct style
- [ ] Bio prose is 2–4 sentences, light weight, correct color
- [ ] Section visible in page after Hero
- [ ] `tsc --noEmit` exits 0
- [ ] `next build` exits 0
